ENV_FILE=.env.local

DB_ROOT_PASSWORD=fshoot
DB_NAME=fruitshoot
DB_USER=appuser
DB_PASSWORD=fshoot

# backend runs INSIDE docker, so it reaches mysql by service name + container port
DB_HOST=mysql
DB_PORT=3306